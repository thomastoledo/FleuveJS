// import { map } from "../../transform/map";
// import { fork } from "./fork";
// import {of} from './of';

describe('fork', () => {
    it('will succeed', () => expect(true).toBe(true));

    // it('should create an observable fork', () => {
    //     const obs$ = fork(of(12, 13), map(x => x * 2));
    //     // obs$.
    // });
});